#!/bin/sh
# Framework fragment: React

tmpl_fw_react() {
    cat << 'EOF'
- Use functional components with hooks
- Keep components small and focused
- Lift state up only when necessary
- Use React.memo sparingly; profile before optimizing
EOF
}
