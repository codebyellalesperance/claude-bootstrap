#!/bin/sh
# Framework fragment: Vue

tmpl_fw_vue() {
    cat << 'EOF'
- Use Composition API with `<script setup>`
- Follow Vue style guide priority rules
- Use computed properties for derived state
EOF
}
