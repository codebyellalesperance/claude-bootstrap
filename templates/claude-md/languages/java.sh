#!/bin/sh
# Language fragment: Java

tmpl_lang_java() {
    cat << 'EOF'
- Follow standard Java conventions
- Use meaningful names; avoid abbreviations
- Prefer constructor injection over field injection
- Use records for immutable data carriers
EOF
}
